# Enable RewriteEngine
RewriteEngine On

# Make sure it's not a real directory or file
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# If URL matches /something, try to serve /something.php
RewriteRule ^(.+)$ $1.php [L]

ErrorDocument 404 /404.html